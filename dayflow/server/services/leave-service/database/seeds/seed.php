<?php

declare(strict_types=1);

// The kernel looks for a seeder at database/seeds/seed.php on every boot. The
// seeder itself lives one level up, next to the migrations whose tables it
// fills, so the two stay together when either is being read or changed.
require __DIR__ . '/../seed.php';
