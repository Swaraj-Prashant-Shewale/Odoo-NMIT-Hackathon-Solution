<?php

declare(strict_types=1);

// The kernel loads database/seeds/seed.php on boot; the seed itself lives one
// directory up, alongside the migrations it depends on.
require __DIR__ . '/../seed.php';
